# 원하는 조건을 만족하는 가장 알맞은 값을 찾으시오 -> 이진 탐색 유형
import sys
n, target = map(int, sys.stdin.readline().split())
array = list(map(int, sys.stdin.readline().split()))
array.sort()

start = 0
end = max(array)

result = 0
while (start <= end):
    mid = (start + end) // 2
    what = 0
    for i in array:
        if i - mid > 0:
            what += i - mid
    if what < target:
        end = mid-1
    else:
        result = mid
        start = mid+1

print(result)

# 4 6
# 19 15 10 17
